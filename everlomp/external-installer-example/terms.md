# Dummy Example App terms

This package is only a demonstration of the Everlomp external-installer format.

Its `install.sh` runs as root and deliberately replaces the public web-root contents while preserving `/var/www/html/install.php` so the setup wizard can continue. Upload and run external installer packages only when you trust their source and have reviewed their scripts.
